import React, { FC } from "react";
import "./Loader.scss";

const Loader: FC = () => {
    return (
        <div className='loader'>
            <h1>Loading...</h1>
        </div>
    );
};

export default Loader;
